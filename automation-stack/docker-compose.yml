name: automation-stack
    
networks:
    default:
        name: home-net
        external: true

x-logging: &default-logging
    driver: json-file
    options:
        max-size: "10m"
        max-file: "3"

services:

# Movies/Shows/Anime Services
    radarr:
        image: lscr.io/linuxserver/radarr:latest
        container_name: radarr
        environment:
            PUID: ${PUID}
            PGID: ${PGID}
            TZ: ${TZ}
        volumes:
            - ./radarr:/config
            - ${SANDBOX_PATH}:/sandbox
            - ${MOVIES_PATH}:/movies      
            - ${DOWNLOADS_PATH}:/downloads  
        ports:
            - "${RADARR_PORT:-6001}:7878"
        logging: *default-logging
        restart: unless-stopped

    radarr-anime:
        image: lscr.io/linuxserver/radarr:latest
        container_name: radarr-anime
        environment:
            PUID: ${PUID}
            PGID: ${PGID}
            TZ: ${TZ}
        volumes:
            - ./radarr-anime:/config
            - ${SANDBOX_PATH}:/sandbox
            - ${ANIME_MOVIES_PATH}:/anime_movies        
            - ${DOWNLOADS_PATH}:/downloads  
        ports:
            - "${RADARR_ANIME_PORT:-6002}:7878"
        logging: *default-logging
        restart: unless-stopped

    sonarr:
        image: lscr.io/linuxserver/sonarr:latest
        container_name: sonarr
        environment:
            PUID: ${PUID}
            PGID: ${PGID}
            TZ: ${TZ}
        volumes:
            - ./sonarr:/config
            - ${SANDBOX_PATH}:/sandbox
            - ${SHOWS_PATH}:/shows
            - ${DOWNLOADS_PATH}:/downloads  
        ports:
            - "${SONARR_PORT:-6003}:8989"
        logging: *default-logging
        restart: unless-stopped

    sonarr-anime:
        image: lscr.io/linuxserver/sonarr:latest
        container_name: sonarr-anime
        environment:
            PUID: ${PUID}
            PGID: ${PGID}
            TZ: ${TZ}
        volumes:
            - ./sonarr-anime:/config
            - ${SANDBOX_PATH}:/sandbox
            - ${ANIME_SERIES_PATH}:/anime_series
            - ${DOWNLOADS_PATH}:/downloads  
        ports:
            - "${SONARR_ANIME_PORT:-6004}:8989"
        logging: *default-logging
        restart: unless-stopped

# Subtitle Services for Regular/Anime Media
    bazarr: 
        image: lscr.io/linuxserver/bazarr:latest
        container_name: bazarr
        environment:
            PUID: ${PUID}
            PGID: ${PGID}
            TZ: ${TZ}
        volumes:
            - ./bazarr:/config
            - ${SANDBOX_PATH}:/sandbox
            - ${MOVIES_PATH}:/movies
            - ${SHOWS_PATH}:/shows
        ports:
            - "${BAZARR_PORT:-6005}:6767"
        logging: *default-logging
        restart: unless-stopped

    bazarr-anime: 
        image: lscr.io/linuxserver/bazarr:latest
        container_name: bazarr-anime
        environment:
            PUID: ${PUID}
            PGID: ${PGID}
            TZ: ${TZ}
        volumes:
            - ./bazarr-anime:/config
            - ${SANDBOX_PATH}:/sandbox
            - ${ANIME_MOVIES_PATH}:/anime_movies
            - ${ANIME_SERIES_PATH}:/anime_series
        ports:
            - "${BAZARR_ANIME_PORT:-6006}:6767"
        logging: *default-logging
        restart: unless-stopped
        
# Indexing Services - Flaresolverr for Cloudflare
    prowlarr:
        image: lscr.io/linuxserver/prowlarr:latest
        container_name: prowlarr
        environment:
            PUID: ${PUID}
            PGID: ${PGID}
            TZ: ${TZ}
        volumes:
            - ./prowlarr:/config
        ports:
            - "${PROWLARR_PORT:-6007}:9696"
        logging: *default-logging
        restart: unless-stopped

# Quality Profile Configuration Service
    recyclarr:
        image: ghcr.io/recyclarr/recyclarr
        container_name: recyclarr
        environment:
            PUID: ${PUID}
            PGID: ${PGID}
            TZ: ${TZ}
        volumes:
            - ./recyclarr:/config
        logging: *default-logging
        restart: unless-stopped
